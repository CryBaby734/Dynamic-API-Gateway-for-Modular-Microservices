spring:
  application:
    name: spring-cloud-mastery
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      label: main
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest